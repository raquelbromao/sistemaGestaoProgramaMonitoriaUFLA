<!-- views/pages/about.ejs -->
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <% include partials/head %>
</head>
<body>
  <header>
    <% include partials/navebar %>
  </header>

  <main>
    <div class="container">

      <div class="jumbotron">
      <% if (perfil == 'Monitor') { %> 
       <p>Relatórios de  <%= monitoria.nomeDisciplina %></p>
            <% if (possuiPlano == true) { %>
                <% if (possuiAtividadesRegistradas == true) { %> 
                    <form class="form-inline" action="/monitoriaVigente/gerarRelatorio" method="POST">
                        <div class="form-group">
                            <label>Mês:</label>
                            <select class="form-control" name="mes">
                                <option value="0">Janeiro</option>
                                <option value="1">Fevereiro</option>
                                <option value="2">Março</option>
                                <option value="3">Abril</option>
                                <option value="4">Maio</option>
                                <option value="5">Junho</option>
                                <option value="6">Julho</option>
                                <option value="7">Agosto</option>
                                <option value="8">Setembro</option>
                                <option value="9">Outubro</option>
                                <option value="10">Novembro</option>
                                <option value="11">Dezembro</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Período:</label>
                            <select class="form-control" name="periodo">
                                <option value="2018-01">2018-01</option>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-default btn-primary">Gerar Relatório</button>
                    </form>    
                <% } else { %>
                    Sem atividades registradas! <br>
                    <a class="btn btn-raised btn-primary btn-block" href="/monitoriaVigente/atividades/registrarAtividade">Cadastrar Atividade</a>
                <% } %>
            <% } else { %>
                Sem plano de trabalho cadastrado! <br>
            <% } %>    
       <% }  else { %>
        <h1>Usuário não autorizado!</h1>
       <% } %>
     </div>
      

    </div>
  </main>

  <footer>
  </footer>
</body>
</html>
